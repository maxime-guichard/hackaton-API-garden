{% extends 'layout.html.twig' %}

{% block stylesheet %}

	<link rel="stylesheet" href="/assets/css/recipes.css">

{% endblock %}

{% block content %}
<div class="container pt-5">
    <div class="row p-5">    
        <div class="col-xl-4 col-lg-6 col-sm-12">
            <div class="card text-white bg-success mb-3">
                <div class="card-header fw-bold">Monday</div>
                    <div class="card-body">
                        <h5 class="card-title">{{ monday.meals.0.strMeal }}</h5>
                        <p class="card-text"><img src="{{ monday.meals.0.strMealThumb }}" width="100px" alt=""/></p><p> {{ monday.meals.0.strInstructions }}</p>
                  </div>
            </div>
        </div>
         <div class="col-xl-4 col-lg-6 col-sm-12">
            <div class="card text-white bg-success mb-3">
                <div class="card-header fw-bold">Tuesday</div>
                    <div class="card-body">
                        <h5 class="card-title">{{ tuesday.meals.0.strMeal }}</h5>
                        <p class="card-text"><img src="{{ tuesday.meals.0.strMealThumb }}" width="100px" alt=""/></p><p> {{ tuesday.meals.0.strInstructions }}</p>
                  </div>
            </div>
        </div>
         <div class="col-xl-4 col-lg-6 col-sm-12">
            <div class="card text-white bg-success mb-3">
                <div class="card-header fw-bold">Wednesday</div>
                    <div class="card-body">
                        <h5 class="card-title">{{ wednesday.meals.0.strMeal }}</h5>
                        <p class="card-text"><img src="{{ wednesday.meals.0.strMealThumb }}" width="100px" alt=""/></p><p> {{ wednesday.meals.0.strInstructions }}</p>
                  </div>
            </div>
        </div>
    </div>
</div>
{% endblock %} 
